name: l10n
description: A sample command-line application.
version: 1.0.0
publish_to: none
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.13.4

resolution: workspace

# Add regular dependencies here.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any


dev_dependencies:
  # build_runner: any
  lints: ^6.0.0
  test: ^1.25.6

flutter: # not required here as we moved that in top root workspace
  generate: true
